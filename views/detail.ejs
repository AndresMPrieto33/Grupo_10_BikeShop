<!DOCTYPE html>
<html lang="en">
        <!-- ## HEAD ## -->
        
        <%- include('partials/head', {title: detalle.name}) %>
        <!-- ## HEAD ## -->
<body>
        <!--  -->
        <%- include('partials/header'); %>
        <!--  -->
        <section class="detalle-producto">
                <div class="detalle-img">
                <img src="/images/products/<%=  detalle.image %>" alt="Foto de <%= detalle.name %>">
                </div>
                <div class="contenedor">
                <h2><%=  detalle.name %></h2>
                <ul class="lista">
                        <p class="lista"><%=  detalle.description %></p>                     
                        <p>$<%=  detalle.price %></p>
                </ul> 
                <div class="boton-comprar-detalle">
                        <button><a href="/products/cart">Añadir al Carrito</a></button>
                </div>
                <div class="boton-comprar-detalle">
                        <button><a href="/products/edit/id">editar producto</a></button>
                </div>

                <form action="/products/delete/<%= detalle.id %>" method="POST" style="display: inline-flex">
                        <button type="submit" class="action-button delete">ELIMINAR</button>
                </form>
                </div>
        </section>
        <!-- ## footer ## -->
        <%- include('partials/footer') %>
        <!-- ## footer ## -->
</body>
</html>