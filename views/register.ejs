<!DOCTYPE html>
<html lang="en">
        <!-- ## HEAD ## -->
        <%- include('partials/head') %>
        <!-- ## HEAD ## -->
<body>
    <!-- ### HEADER ### -->
    <%- include('partials/header') %>
    <!-- ### HEADER ### -->

<main class="register">
    <form action="/user/storage" method="POST"  enctype="multipart/form-data">
        <div class="personal-info">
        <input type="text" id="firstName" name="firstName" placeholder="Nombre" class="form-control <%= locals.errors && errors.firstName ? 'is-invalid' : null %>" value="<%= locals.oldData ? oldData.firstName : null %>"> 
        <% if (locals.errors && errors.firstName) { %>
            <div class="error-text"><%= errors.firstName.msg %></div>                     
        <% } %>
    </div>
            <br>
            <div class="personal-info">
                <input type="text" name="lastName" placeholder="Apellido" class="form-control <%= locals.errors && errors.lastName ? 'is-invalid' : null %>" value="<%= locals.oldData ? oldData.lastName : null %>"> 
                <% if (locals.errors && errors.lastName) { %>
                    <div class="error-text"><%= errors.lastName.msg %></div>                     
                <% } %>
            </div>
            <br>
            <div class="personal-info">
                <input type="text" name="email" placeholder="ejemplo@undostres.com" class="form-control <%= locals.errors && errors.email ? 'is-invalid' : null %>" value="<%= locals.oldData ? oldData.email : null %>"> 
                <% if (locals.errors && errors.email) { %>
                    <div class="error-text"><%= errors.email.msg %></div>                     
                <% } %>
            </div>
            <br>
            <div class="personal-info">
                <input type="text" name="address" placeholder="Dirección" class="form-control <%= locals.errors && errors.address ? 'is-invalid' : null %>" value="<%= locals.oldData ? oldData.address : null %>"> 
                <% if (locals.errors && errors.address) { %>
                    <div class="error-text"><%= errors.address.msg %></div>                     
                <% } %>
            </div>
        </div>
        <!-- //Chequear si lo siguiente es necesario: -->

        <h4>Perfil del usuario</h4>
        

        <h4>Marque lo que mejor se adapte a usted:</h4>
        <br>
        <div class="caracteristicas">
            <div>
                <input type="checkbox" name="caracteristicas" value="a" id="a">
                <label for="a">Quiero comenzar en el mundo del ciclismo</label>
            </div>
            <div>
                <input type="checkbox" name="caracteristicas" value="b" id="b">
                <label for="b">Soy un ciclista principante</label>
            </div>
            <div>
                <input type="checkbox" name="caracteristicas" value="c" id="c">
                <label for="c">Soy un ciclista experimentado</label>
            </div>
            <div>
                <input type="checkbox" name="caracteristicas" value="d" id="d">
                <label for="d">Soy un ciclista profesional y competitivo</label>
            </div>
        </div>

        <!-- <h4>Foto de perfil</h4>
        <input type="file" name="avatar">
        <br>
        <br> -->
        <div class="password">
            <div class="personal-info">
                <input type="password" name="pass" placeholder="Creá tu contraseña" class="form-control <%= locals.errors && errors.password ? 'is-invalid' : null %>" value="<%= locals.oldData ? oldData.password : null %>"> 
                <% if (locals.errors && errors.password) { %>
                    <div class="error-text"><%= errors.password.msg %></div>                     
                <% } %>
            </div>
            <br>
            <!-- <div class="personal-info">
                <input type="password" name="pass_confirm" placeholder="Ingresá nuevamente tu contraseña">
            </div> -->
            <br>
            <br>
        </div>
        <div class="campo">
            <div class="tag">
                <label for="description" class="form-label">Imagen de perfil:</label>
                    <input name="image" id="image" class="form-input" type="file">

            </div>
        <div class="botones">                    
            <input type="submit" value="Enviar" class="submit">
            <input type="reset" value="Borrar">
        </div>
    </form>
</main>

            <!-- ## footer ## -->
            <%- include('partials/footer') %>
            <!-- ## footer ## -->
</body>
</html>