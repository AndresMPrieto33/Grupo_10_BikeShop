<!DOCTYPE html>
<html>
        <!-- ## HEAD ## -->
        <%- include('partials/head') %>
        <!-- ## HEAD ## -->
    <body>
        <!-- ## header ## -->
        <%- include('partials/header') %>
        <!-- ## header ## -->

        <% for( let i = 0; i < parts.length; i++ ) { %>            
            <div class="productsAll">
                <section class="productos-novedades">
                    <div class="novedades">
                        <a href="/products/detail/<%= parts[i].id  %>"><img src="/img/products/<%= parts[i].image %>" alt="Imagen Bicicleta"></a>
                        <div class="detalle">
                            <div class="name"><h4><%= parts[i].name %></h4></div>
                            <div class="description"><p><%= parts[i].description %></p></div>
                            <div class="precio"><p>$<%= parts[i].price %></p></div>
                            <section class="boton-comprar-detalle">
                                <button><a href="/products/cart">Añadir al Carrito</a></button>
                        </section>                        
                    </div>
                    </div>
                </section>           
            </div>
        <% } %>

        <!-- ## footer## -->
        <%- include('partials/footer') %>
        <!-- ## footer## -->
    </body>
</html>